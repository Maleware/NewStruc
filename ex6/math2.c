#include <stdio.h>
/*
#include <stdlib.h>
#include <tgmath.h>

int math2(void)
{
	float f=1.1;
	double d=2.2;
	long double ld=3.3;
	float complex fc= 1.0 + 2.0*I, fcval;
	double complex dc=4.0 + 2.0*I, dcval;
	long double complex ldc=8.0 + 9.0*I, ldcval;

	//Calc with reel numbers
	
	printf("Squareroot of %f = %f\n",f,sqrt(f));
	fprintf(stdout, "Squareroot of %f = %f\n",d,sqrt(d));
	fprintf(stdout, "Squareroot of %Lf = %Lf\n", ld,sqrt(ld));
	fcval=sqrt(fc);
	fprintf(stdout, "Squareroot of %f + %fi = %Lf\n", creal(fcval), cimag(fcval), fcval);
	dcval=sqrt(dc);
	fprintf(stdout, "Squareroot of %f + %fi = %Lf\n", creal(dcval), cimag(dcval), dcval);
	ldcval=sqrt(ldc);
	fprintf(stdout, "Squareroot of %Lf + %Lfi = %Lf\n", creal(ldcval), cimag(ldcval), ldcval);
	return EXIT_SUCCESS;
}

int ex62(void)
{
	return math2();
}
*/
